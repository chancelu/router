# 火山引擎 TOS 配置模板（复制为 .env 后修改）
# 注意：本文件仅为示例，不会被程序读取。请复制为 .env 并填写实际值。

# 图床选择：auto | volc | imgbb | smms(占位)
# - auto：若配置了 TOS（volc），优先用 TOS；否则用 imgbb
# - volc：强制使用火山引擎 TOS
# - imgbb：使用 imgbb 图床，需要 IMGBB_API_KEY
IMAGE_HOST=auto

# 可选：如果你有后端的公网域名/隧道地址，用于返回本地 uploads 的公网 URL
# 示例：PUBLIC_SERVER_ORIGIN=https://your-server.example.com
PUBLIC_SERVER_ORIGIN=

# 备用：IMGBB 图床（若不配置 TOS 则可用）
# 在 https://api.imgbb.com/ 申请 API Key
IMGBB_API_KEY=

# 火山引擎 TOS（对象存储）配置
# 控制台：访问控制 -> 访问密钥 -> 创建访问密钥，获得 AK/SK
VOLC_ACCESS_KEY_ID=
VOLC_SECRET_ACCESS_KEY=

# TOS 的桶名与地域（region 例：cn-beijing / cn-north-1 等）
VOLC_BUCKET=
VOLC_REGION=cn-beijing

# S3 兼容 Endpoint（用于 SDK 上传）
# 示例： https://tos-s3-cn-beijing.volces.com
VOLC_ENDPOINT=

# 若你给桶绑定了自定义 CDN/域名，填写它以得到更美观的公开 URL
# 例如：https://img.example.com
VOLC_PUBLIC_HOST=

# =========================
# 可选：AI 提供商密钥（若你要使用豆包/其他兼容服务）
# DOUDAO_API_KEY=
# OPENAI_API_KEY=
# OPENROUTER_API_KEY=
# DASHSCOPE_API_KEY=

# =========================
# 开发/测试辅助项
# MOCK_FENGSHUI=0