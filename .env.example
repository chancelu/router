# 将本文件复制为 .env 并填写密钥
OPENAI_API_KEY=
OPENROUTER_API_KEY=
AZURE_OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
DEEPSEEK_API_KEY=
DASHSCOPE_API_KEY=
DOUDAO_API_KEY=
OPENAI_COMPAT_API_KEY=
PORT=3001
# 图床配置：默认使用 imgbb
# 可选：imgbb | volc | smms | auto（若配置了火山TOS则优先volc，否则imgbb）
IMAGE_HOST=auto
IMGBB_API_KEY=
# 火山引擎 TOS（S3 兼容）配置
VOLC_ACCESS_KEY_ID=
VOLC_SECRET_ACCESS_KEY=
VOLC_BUCKET=
VOLC_REGION=cn-beijing
# TOS 公开访问域名（可选，若使用自定义 CDN/域名）
VOLC_PUBLIC_HOST=
# TOS 兼容 endpoint（可选，默认 https://tos-<region>.volces.com）
VOLC_ENDPOINT=
# 备用国内图床（未来可选）：sm.ms
# SMMS_API_TOKEN=
# 如果部署到公网，设置对外可访问的站点地址（用于本地回退时生成绝对URL）
PUBLIC_SERVER_ORIGIN=
